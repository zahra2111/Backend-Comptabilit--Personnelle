<form method="post" action="{{ path('app_bank_delete', {'id': bank.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bank.id) }}">
    <button class="btn">Delete</button>
</form>
