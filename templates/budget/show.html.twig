{% extends 'base.html.twig' %}

{% block title %}Budget{% endblock %}

{% block body %}
    <h1>Budget</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ budget.id }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ budget.montant }}</td>
            </tr>
            <tr>
                <th>APartirDe</th>
                <td>{{ budget.APartirDe ? budget.APartirDe|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ budget.duree }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_budget_index') }}">back to list</a>

    <a href="{{ path('app_budget_edit', {'id': budget.id}) }}">edit</a>

    {{ include('budget/_delete_form.html.twig') }}
{% endblock %}
